# Ensure Vercel installs devDependencies even with NODE_ENV=production.
# next build requires typescript (devDep) to compile next.config.ts.
production=false

# Keep pnpm strict so the lockfile is authoritative.
engine-strict=true

# Hoist nothing by default — Turborepo likes explicit package boundaries.
shamefully-hoist=false
